dev:

    name: !!! secret environment!!!
    secret: password

    # секретное подключение к БД
    mysql:
        username: root
        password: password
        isDebug: true
        dsn: mysql:host=secrethost;dbname=secretDbName